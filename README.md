## Requirements
NASM and GCC.
after installed and inside your PATH, then run the **make.bat**

In Linux, just <strong><i>sudo apt install nasm gcc</strong></i> or <strong><i>sudo pacman -S nasm gcc</strong></i> 'em and run **make.sh**